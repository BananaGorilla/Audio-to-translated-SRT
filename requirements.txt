# YouTube regularly changes its player protocol.  Install yt-dlp from its
# upstream master branch, which carries extractor fixes before the next stable
# release. This isolates the fast-moving dependency to yt-dlp rather than
# allowing prereleases of every package in this requirements file.
# yt-dlp currently supports curl_cffi 0.10 through 0.15.  0.16 makes the
# optional browser-impersonation handler unavailable.
yt-dlp[default,curl-cffi] @ https://github.com/yt-dlp/yt-dlp/archive/master.tar.gz
curl-cffi>=0.10,<0.16
google-genai
pydub
python-dotenv
PySide6
litellm
openai
llama-cpp-python
